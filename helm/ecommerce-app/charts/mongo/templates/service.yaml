apiVersion: v1
kind: Service
metadata:
  name: mongo-db
  labels:
    app: mongo
spec:
  clusterIP: None
  selector:
    app: mongo
  ports:
    - port: {{ .Values.port }}
      targetPort: {{ .Values.port }}
